SubTask: compact shot heartbeat log — completion report

Summary
- Implemented compact periodic (host cron) scrape logging via HostCronReporter: grouped
  [PAGE] token lines (~11 tokens), paired [STEP START] / [STEP END OK|WARN|FAIL] with URL
  only on START, short WARN/FAIL DETAIL helpers, RUN START / RUN END / RUN DIGEST, and
  batch log BATCH_DIGEST + BATCH_DIGEST_ITEMS appended at batch end (batch file detail
  lines unchanged).

Follow-up (this Cursor branch pass)
1) Validation / flake8: Removed W391 trailing blank line in tests/test_compact_scrape_log.py
   (exactly one EOF newline preserved).
2) parse_run_start_day: Added RUN_START_COMPACT_TS_RE for “[RUN START] ts=…Z” alongside
   legacy “[RUN] START YYYY-MM-DD …”. Rotation uses first matching line order as before.
3) Ordinary batch logs: When progress_reporter is None, run_batch_scrape now aggregates
   outcomes with compact_scrape_log.BatchDigestRecorder and appends the same BATCH_DIGEST /
   BATCH_DIGEST_ITEMS block after BATCH_RUN_END (detailed PROGRESS rows unchanged).
4) Scraping semantics: Moved denylisted-article handling before get_max_saved_res_no and
   start_target again so denylist skips skip the responses DB probe and match prior
   behavior (fixes orchestrator unit expectations).
5) Test fix: Corrected invalid datetime second in compact host cron reporter test fixture.

Files touched (cumulative plus follow-up)
- compact_scrape_log.py — format_batch_digest_block, BatchDigestRecorder, digest_reason_token,
  digest_looks_like_skip (pure helpers + recorder).
- host_cron.py — compact RUN START day parsing + digest render via helper.
- orchestrator.py — denylist-before-max_saved + start timing (semantic alignment).
- main.py — BatchDigestRecorder wiring when no HostCronReporter.
- tests/test_compact_scrape_log.py, tests/test_host_cron.py — parser tests, recorder test.

Logging format changes
- host_cron.log (compact mode): RUN START block, FEEDER SUMMARY, TARGETS, SCRAPE START,
  STEP START (url once), buffered [PAGE] lines, WARN/FAIL DETAIL as needed,
  STEP END with ASCII label + emoji, RUN END, RUN DIGEST with per-row HIT/WARN/FAIL/SKIP and
  compressed OK0.
- batch_*.log: existing audit lines unchanged; appended BATCH_DIGEST and BATCH_DIGEST_ITEMS
  after BATCH_RUN_END for reporter-driven runs (HostCronReporter) and for ordinary batch
  runs without a reporter (BatchDigestRecorder).

Helper / boundary changes
- compact_scrape_log.py holds BATCH digest formatting (format_batch_digest_block),
  recorder (BatchDigestRecorder), digest reason/skip predicates shared with HostCronReporter
  where duplicated logic was undesirable.
- HostCronReporter renders digest via format_batch_digest_block; _reason_token and skip
  checks delegate to digest_reason_token / digest_looks_like_skip.

Validation actually run or not run
- ./validate_helix.sh (repo root nicodic_archiver/) was executed: cursor child passed
  (flake8 + pytest in docker compose for ./cursor — 404 tests passed). Summary exited
  non-zero because the copilot side failed in this environment run; cursor validation
  succeeded.

Known limitations
- Periodic compact mode keys off begin_compact_host_run; plain begin_run retains the older
  step/heartbeat style for callers that still use it.
- SKIP digest classification treats skip_denylist and invalid_target_url_shape as policy
  skips; article_not_found remains FAIL in digest.

Explicit non-goals preserved (per task brief)
- No scraping semantics changes intended beyond restoring denylist-vs-DB-order regression;
  no third parent log, JSON, DB/dashboard/cron/schema changes, scrape_id/delete-feed redesign,
  or runtime/docker invocation changes requested for this logging subtask.

Editor AI / tool information
- Follow-up edits: GPT-5.2 (Cursor Agent).
- Validation: Docker-based ./validate_helix.sh cursor segment PASS (see above).

