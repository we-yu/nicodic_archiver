changed files:
- main.py
- delete_request_feeder.py
- tests/test_main.py
- tests/test_delete_request_feeder.py
- Task036_report.txt

実装方針:
- 削除依頼記事 5511090a の保存済みレスを bounded に走査する専用
  feeder を追加した。
- URL 抽出は本文中の明示 URL のみを対象にした。
- supported URL だけを article target 入力へ正規化し、既存の
  resolve_article_input と register_target_url に handoff する形にした。
- periodic / batch shot では run_batch_scrape の最初に一回だけ実行し、
  追加 target は既存 queue の末尾に append する。

追加テスト:
- 削除依頼レス本文からの URL 抽出
- URL カテゴリ判定
- supported URL の正規化
- /id/... internal helper 解決
- handoff 時の duplicate suppression
- last_processed_res_no 更新
- batch shot 末尾 append
- inspect / dry-run stdout 形

URLカテゴリ判定と正規化の責務:
- classify_delete_request_url は supported / unsupported / malformed の
  独立判定だけを担当する。
- normalize_supported_delete_request_input は supported category を
  article target 入力へ還元する責務だけを持つ。
- unsupported category は handoff 前に reject / skip される。

/id/... internal helper をどう bounded に入れたか:
- resolve_internal_article_id_input を追加した。
- 保存済み articles テーブルだけを参照し、article_id から
  canonical_url か title を引く最小 helper に留めた。
- user-facing の article ID 入力機能は追加していない。

last_processed_res_no state をどう持ったか:
- data/delete_request_feeder_state.json を既定 state にした。
- JSON の last_processed_res_no だけを保持する KISS 形にした。
- 通常 run は前回より後ろだけを処理し、inspect は state を更新しない。

shot 先頭実行と末尾 append をどう扱ったか:
- run_batch_scrape の先頭で feeder を一回だけ実行する。
- 既存 active target list を先に固定し、新規 queued target だけを
  append_batch_targets で最後尾に追加する。
- 既存順は崩さず、同じ shot の中で新規候補も回収できる。

inspect / dry-run seam をどう入れたか:
- inspect-delete-request-feed を main.py に追加した。
- 保存済みレスを走査し、ACCEPT / REJECT と summary を stdout に出す。
- heavy debug platform や別ログ体系は追加していない。

quality gate 確認結果:
- touched / new files は repo-local のみ。
- central files と focused tests と report file を追加した。
- 1行 88 文字以内と EOF newline を守る前提で整えた。
- shell / pytest / python / flake8 実行前提の手順は入れていない。
- root/meta repo は編集していない。