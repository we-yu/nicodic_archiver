Summary
- Registered Articles read-side sorting now uses typed ORDER BY logic for
  numeric and datetime-like fields while keeping the default sort as
  Created At descending.
- Registered Articles web UI now shows JST-formatted table times, gives the
  Title column more visual weight, truncates long Canonical URL display in the
  table, widens the search field, and adds Reset and Refresh actions.
- Registered Articles layout was tightened so long URLs stay single-line and
  ellipsized inside their column, titles wrap safely, alignment matches data
  type, and pagination now appears above and below the table.
- Registered Articles table header and body cells now use consistent vertical
  centering so wrapped Title rows no longer pull neighboring cells upward.
- CSV export behavior was preserved for full Canonical URL values and existing
  machine-friendly date values.

Files touched
- archive_read.py
- web_app.py
- tests/test_archive_read.py
- tests/test_web_app.py

Tests added or updated
- Added registered-article sort coverage for numeric Article ID values.
- Added numeric sort coverage for Saved Responses and Max Res No.
- Added datetime-like sort coverage for Created At and Last Scraped, including
  missing Last Scraped ordering.
- Added web HTML coverage for JST table display, URL truncation class/full href
  preservation, Reset link, Refresh link, wider search input, and CSV full URL
  preservation.
- Added focused web HTML coverage for top pagination, alignment classes, and
  title/URL layout styles.
- Added a focused web HTML/style check for middle vertical alignment on table
  cells.

Validation attempted or not attempted
- Executable validation: Not run.
- Static inspection only: checked touched Python files with editor error
  reporting, inspected for over-88-character lines, reviewed touched diffs,
  and updated the web HTML/style assertion coverage.

Known limitations
- Max Res No semantic redesign was intentionally not changed in this SubTask.
  The column still sorts numerically using the current existing read-side value
  derived from locally saved responses.
- CSV date formatting was preserved; web-only table display was changed to JST.
- Refresh is implemented as a simple state-preserving link. No JavaScript was
  added.

Quality notes
- The 88-character line limit was checked for all touched Python files.
- EOF newline was inspected for touched/new files.
- Unused variables were checked via editor static inspection on touched Python
  files.
- Unrelated repository changes were not modified.

Editor AI model/tool information
- GitHub Copilot using GPT-5.4.
- Tools used: workspace file edits, workspace search, git change inspection,
  and editor static error inspection.

Follow-up layout tweak
- Adjusted Registered Articles column widths from Title 19% / URL 15% to Title 21% / URL 13% so long system article titles fit better while URL ellipsis behavior remains intact.
