Editor AI model: GitHub Copilot GPT-5.4

Summary
- Added bounded lock-tolerance around scrape-run observation persistence.
- Only lock-style sqlite3.OperationalError cases for scrape_run_observation
  append are downgraded to warnings.
- Archive-critical persistence, target registry writes, and scrape execution
  semantics remain unchanged.

Files changed
- main.py
- tests/test_main.py
- SubTask-BugFix-observation-db-lock-tolerance_report.txt

Behavior change
- Batch and periodic target processing now continue when
  append_scrape_run_observation(...) hits a SQLite lock or busy style
  OperationalError.
- The skipped telemetry append emits a bounded warning to the existing
  human-visible path and is also recorded in the batch log as a
  TELEMETRY_WARNING block.
- Non-lock sqlite3.OperationalError cases in this telemetry path remain fatal.

Why archive-critical writes remain fatal
- The tolerance is applied only at the main.py observation recording boundary.
- storage.save_to_db(...) and the scrape/archive persistence path were not
  changed.
- Target registry and redirect handoff write paths were not changed.
- Non-lock OperationalError in observation recording is still re-raised.

How telemetry lock failures are surfaced
- A short warning line is emitted through stdout batch logging, or through the
  progress reporter warning path when present.
- The batch log receives a TELEMETRY_WARNING detail block with run kind,
  article identity, and the SQLite error detail.
- No traceback spam is added for expected lock-style skips.

Tests added or updated
- Added a focused test proving non-lock sqlite3.OperationalError in the
  observation path is still fatal.
- Added a focused batch test proving telemetry append lock failure does not
  abort otherwise successful target processing.
- Existing batch failure coverage remains in place for scrape exceptions.

Validation status if available
- docker compose run --rm --entrypoint "" scraper pytest -q tests/test_main.py
  -k "observation or telemetry_once_per_target or
  exception_sets_nonzero_exit_and_continues"
  -> 4 passed, 75 deselected in 0.83s
- docker compose run --rm --entrypoint "" scraper flake8 main.py
  tests/test_main.py
  -> passed with no output

Known limitations
- This change does not retry telemetry writes.
- This change does not alter SQLite connection timeout, journal mode,
  busy_timeout, or schema behavior.
- Only lock or busy style OperationalError cases are downgraded in the
  observation append path.